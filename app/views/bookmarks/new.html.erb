<h1>Adding bookmark for <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, prompt: "Choose a movie.." %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
